/**
 * Inbox: default landing zone for quick-captured items.
 * See DESIGN.md Section 4.4 (Search & Organization — Inbox).
 */
export function InboxPage() {
  return (
    <div className="page">
      <h1>Inbox</h1>
      <p className="placeholder">Quick-captured cards will appear here. Create one with <kbd>Alt+N</kbd> or the + button.</p>
    </div>
  )
}
